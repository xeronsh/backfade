# Goal

**Status**: active
**Policy**: goal
**Auto-continue**: on
**File**: `20260914065800-fmqoho.md`

## Objective

> Build Backfade v0.1 complete loop per BACKFADE_GOAL.md, from empty repo, in three parts per spec §5–7: (1) Foundry contracts — ThesisFactory + ThesisMarket + OracleMath + MockUSDG, WEIGHTED_BASKET_EXCESS_RETURN settlement via mock Chainlink-compatible feeds, oracle safety cases per §5.9, fuzz + invariant tests, Deploy.s.sol + CreateDemoMarket.s.sol scripts; (2) Python FastAPI Thesis Compiler — POST /v1/thesis/compile + GET /v1/assets + /health, OpenAI-compatible structured-output client reading env key with deterministic mock fallback when key absent, fail-closed whitelist validator (symbols→feed mapping from api/data/assets.json, never from LLM), error contract per §6.10; (3) Vite vanilla-TS + Tailwind web — 4 pages (feed/create/market/profile) multi-page build, no React/wagmi/router, viem + EIP-1193, BACK/FADE pari-mutuel UI, conviction bar, transaction flow per §12.3. End-to-end demo on local Anvil: deploy → create market with creator bond → BACK → FADE → resolve → claim, receipts recorded. Deploy scripts written for Robinhood testnet (chain 46630) but NOT deployed — no private keys in repo. Out of scope: everything in spec §3.2 (no DB, no indexer, no secondary trading, no account abstraction), no testnet deployment, no real feed addresses.

## Verification contract

1. `cd contracts && forge test` — all suites pass with 0 failures, including fuzz and invariant test files (ThesisMarketFuzz.t.sol, ThesisMarketInvariant.t.sol exist and run).
2. Contracts: `grep -c "event PositionTaken\|event MarketResolved\|event MarketCancelled\|event Claimed\|event MarketCreated" contracts/src/ThesisMarket.sol` returns all 5 events; ThesisMarket.sol contains no owner/admin settlement function.
3. Backend: with no LLM key in env, start uvicorn and `POST /v1/thesis/compile {"text":"AI is rotating into nuclear energy.","preferred_duration_days":30}` returns 200 with ThesisSpec JSON — weights sum to 10000 bps, basket ≤5, hurdle in 100–5000 bps, feed addresses come from assets.json (not LLM).
4. `GET /v1/assets` returns the demo universe; invalid compile input (weights ≠ 10000) returns the §6.10 error envelope `{"error":{"code":"THESIS_INVALID",...}}` with 4xx, no stack trace.
5. `cd web && npm run build` succeeds producing index.html, create.html, market.html, profile.html; grep confirms no react/wagmi/vue dependency in web/package.json.
6. Anvil end-to-end: `cd contracts && forge script script/E2E.s.sol --fork-url http://localhost:8545 --broadcast` (or equivalent demo script) completes create→back→fade→resolve→claim and prints a tx receipt per step, final claim payout > 0.
7. Repo hygiene: `.gitignore` covers `.env*`; `git grep -iE "0x[a-fA-F0-9]{64}"` finds no committed private key; no chain 46630 deployment broadcast in git history.
